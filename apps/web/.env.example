# URL de la API. Por defecto (si no se define) el cliente usa http://localhost:3001,
# que coincide con el puerto de la API tanto en docker-compose como en desarrollo local.
NEXT_PUBLIC_API_URL=http://localhost:3001
